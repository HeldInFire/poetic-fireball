<div class="closure">
  <slot />
</div>

<style>
.closure {
  font-size: 1.05rem;          /* slightly larger than body */
  line-height: 1.55;           /* readable, airy */
  margin-block: var(--space-l) var(--space-m); /* extra top, moderate bottom spacing */
  color: var(--text-secondary); /* subtle emphasis */
  font-style: italic;          /* optional for reflective tone */
}
</style>
